var animation = function () {
  var n, i, t = function () {
      n = document.querySelectorAll(".fadeIn"), i = window.innerHeight, e()
    },
    e = function () {
      window.addEventListener("scroll", o), window.addEventListener("load", o), window.addEventListener("resize", t)
    },
    o = function () {
      for (var t = 0; t < n.length; t++) {
        var e = n[t].getBoundingClientRect().top;
        i > e + 100 && n[t].classList.add("active")
      }
    };
  return {
    init: t
  }
};
animation().init();